spec:
  containers:
  - name: nginx
    image: nginx:1.27
    resources:
      requests:        # guaranteed minimum
        cpu: "100m"    # 100 millicores = 0.1 CPU
        memory: "128Mi"
      limits:          # maximum allowed
        cpu: "500m"
        memory: "256Mi"
